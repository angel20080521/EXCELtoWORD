1.我想做一个WEB小程序，可以将我的EXCEL文件中的字符和数值，自动填入到一个有规定格式的WORD文件中，并对WORD文件中的表格格式进行合理化调整，该WORD文件的文件名以EXCEL文件中的部分字符组成，并自动保存到EXCEL源文件的目录中，并提供可以选择其它目录的选项。

2.第1页A1要求宋体加黑1号字，
第1页A3要求宋体加黑4号字并居中，
第1页L3请宋体加黑4号字。
第2页C5所在行宋体小四不加粗，
第2页A3所在行宋体小四不加粗，
第2页L3所在行宋体小四不加粗，
第2页H6所在行宋体小四不加粗。
最后一页C5所在行宋体小四不加粗，最后一页A3所在行宋体小四不加粗，最后一页L3所在行宋体小四不加粗，最后一页H6所在行宋体小四不加粗。并优化字体设备，避免文本被分成了多个 run，或者格式被模板覆盖。。

3.excel中有A1、A3、L3、C5、J3、J6、H6，以及从第10行开始的多行数据。 用于生成文件名的部分字符是A1+L3单元格。 word模板的填充位置通过字段名点位符。表格的合理化调整主要是统一列宽、调整字体、设置边框样式 "指定的目录"是指固定的路径，同时还可以支持动态选择。

4.将excel表中最后一非空白行的，D单元格中的数据，以货币格式填入到word文档中WSJEHJ占位符，J单元格中的数据，以货币格式填入到word文档中SEHJ占位符，L单元格中的数据，以货币格式填入到word文档中HSJEHJ占位符。

5.统计税率为13%的金额（未税）合计数，以货币形式填入到SPWSJE占位符；统计税率为13%的税额合计数，以货币形式填入到SPSE占位符；统计税率为13%的金额（含税）合计数，以货币形式填入到SPHSJE占位符。统计税率为6%的金额（未税）合计数，以货币形式填入到FWWSJE占位符；统计税率为6%的税额合计数，以货币形式填入到FWSE占位符；统计税率为13%的金额（含税）合计数，以货币形式填入到SPHSJE占位符。

6.将EXCEL表中自第10行开始的列表内容，通过循环方式，一行一行填充到WORD文档中，C列对应CPMC，D列对应CPMS，E列对应CPSL，F列对应CPDW，G列对应HSDJ并在数据前加￥，I列对应SL，J列对应WSJE并在数据前加￥，L列对应HSJE并在数据前加￥。BH不从EXCEL的A列取值，而是直接使用循环变量，并从1开始。直至A列单元格数值是“未税合计”停止循环。不需要把EXCEL中的行号填充到WORD中，并且不要把EXCEL第9行的数据填充到WORD中。